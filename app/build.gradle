plugins {
    alias(libs.plugins.android.application)
}

// TODO FIXME enhancement 3?
//plugins {
//    id 'androidx.room' version "$room_version" apply false
//}


android {
    namespace 'com.zybooks.projecttwonickburnetteweightlossoption'
    compileSdk 35

    defaultConfig {
        applicationId "com.zybooks.projecttwonickburnetteweightlossoption"
        minSdk 33
        targetSdk 35
        versionCode 1
        versionName "1.0"

        testInstrumentationRunner "androidx.test.runner.AndroidJUnitRunner"
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }
    compileOptions {
        sourceCompatibility JavaVersion.VERSION_11
        targetCompatibility JavaVersion.VERSION_11
    }
}

dependencies {

    implementation libs.appcompat
    implementation libs.material
    implementation libs.activity
    implementation libs.constraintlayout
    testImplementation libs.junit
    androidTestImplementation libs.ext.junit
    androidTestImplementation libs.espresso.core
    // UPDATED artifact 3, add ROOM
    implementation "androidx.room:room-runtime:2.8.1"
    annotationProcessor "androidx.room:room-compiler:2.8.1"
}